# Implementation Plans

Generated by the improve skill on 2026-06-14. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Restore the MCP guidance test baseline | P1 | S | — | DONE |
| 002 | Terminate bash process trees on timeout and cancellation | P1 | M | 001 | DONE |
| 003 | Coalesce streaming UI notifications without losing immediate output | P2 | M | 001 | DONE |
| 004 | Avoid full FTS rebuilds for append-only session saves | P2 | M | 001 | TODO |
| 005 | Reduce mem0 dependency and install fragility | P2 | M/L | 001 | TODO |
| 006 | Document Bun runtime and Memory prerequisites accurately | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale, e.g. finding fixed independently or approach abandoned)

## Dependency Notes

- 001 should land before plans 002-005 because the full `bun test` gate is currently red and later plans need a reliable baseline.
- 002 is independent of 003-005 after 001 lands.
- 003 and 004 both touch session runtime/persistence-adjacent paths but solve different problems; run them on separate branches and rebase the later one.
- 005 may change Memory dependencies and can affect README wording; if 005 lands before 006, re-check the Memory key wording in 006 before executing it.
- 006 is docs-only and can land early, but its final `bun test` done criterion may need plan 001 first.

## Verification Gates From Recon

Use these repo commands unless a plan specifies a narrower focused command first:

| Purpose | Command | Notes |
| --- | --- | --- |
| Typecheck | `bun run typecheck` | Runs `bunx tsc -p tsconfig.json --noEmit` |
| Lint | `bun run lint` | Biome lint check for `src __tests__` |
| Format check | `bun run format:check` | Biome format check, no writes |
| Tests | `bun test` | Currently fails until plan 001 lands |
| Full check | `bun run check` | Typecheck + lint + format check; does not run tests |

## Findings Considered And Rejected

- Default `bashApprovalLevel = "dangerous"`: not planned alone because scoped dangerous-command approval appears to be an intentional product mode; concrete bash safety issues should be handled separately.
- User-configured MCP stdio execution: not planned alone because local MCP server execution is a documented feature.
- Individual low/moderate audit advisories: not split into standalone plans; dependency posture is covered by plan 005, focused on high/critical reachable runtime risk and install fragility.
- Broad avatar render-loop optimization: not selected by the user in this batch.
- Session database permission tightening: not selected by the user in this batch.
